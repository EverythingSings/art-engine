[package]
name = "art-engine-cli"
version = "0.1.0"
edition = "2021"
description = "CLI binary for the art-engine"

[dependencies]
art-engine-core = { path = "../core" }
art-engine-engines = { path = "../engines" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
