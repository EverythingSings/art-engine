[package]
name = "art-engine-differential"
version = "0.1.0"
edition = "2021"
description = "Differential-growth (self-organizing polyline) engine for the art-engine"

[dependencies]
art-engine-core = { path = "../core" }
glam = "0.29"
serde_json = "1"

[dev-dependencies]
proptest = "1"
