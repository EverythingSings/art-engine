[package]
name = "art-engine-mandelbrot"
version = "0.1.0"
edition = "2021"
description = "Mandelbrot escape-time fractal engine for the art-engine"

[dependencies]
art-engine-core = { path = "../core" }
serde_json = "1"

[dev-dependencies]
proptest = "1"
